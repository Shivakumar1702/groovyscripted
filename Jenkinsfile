node{
    if(env.BRANCH_NAME == 'master'){
        stage('build master'){
            echo "Building the Master"
        }
    }
    if(env.BRANCH_NAME == 'dev'){
        stage('build dev'){
            echo "building dev"
        }
    }
}
